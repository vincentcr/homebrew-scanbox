# homebrew-scanbox

Homebrew tap for [scanbox](https://github.com/vincentcr/scanbox) — scanning from
older HP MFPs that macOS dropped support for.

```sh
brew tap vincentcr/scanbox
brew install scanbox
scanbox find
```
